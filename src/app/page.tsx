import { Button } from "@/components/ui/button";

const HomePage = () => {
  return (
    <main>
      <Button variant="destructive" size="lg">
        Click me!
      </Button>
    </main>
  );
};

export default HomePage;
